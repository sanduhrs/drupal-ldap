<?php

/**
 * @file
 * Contains ldap_server.module..
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function ldap_server_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the ldap_server module.
    case 'help.page.ldap_server':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('LDAP Server.') . '</p>';
      return $output;

    default:
  }
}
